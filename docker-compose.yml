name: zero

services:
  db:
    image: postgres:18
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: zero
      POSTGRES_PASSWORD: zero
      POSTGRES_DB: zero
    volumes:
      - db_data:/var/lib/postgresql

volumes:
  db_data:
